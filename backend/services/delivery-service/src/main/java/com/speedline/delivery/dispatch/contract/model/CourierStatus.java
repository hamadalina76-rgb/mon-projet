package com.speedline.delivery.dispatch.contract.model;

/**
 * Contract enum for dispatch availability status.
 * Contract-Version: 1.0
 */
public enum CourierStatus {
    IDLE,
    ON_DELIVERY,
    PRE_ASSIGNABLE
}
