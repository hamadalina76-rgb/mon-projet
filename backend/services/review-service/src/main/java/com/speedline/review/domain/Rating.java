package com.speedline.review.domain;

/**
 * Entité Rating (agrégé) (MongoDB)
 * - id, entityType, entityId, averageRating
 * - totalReviews, ratingDistribution
 */
public class Rating {
    // TODO: Implémenter avec @Document (MongoDB)
}
