package com.speedline.review.domain;

/**
 * Entité Review (MongoDB)
 * - id, orderId, customerId, targetType (PARTNER/COURIER)
 * - targetId, rating (1-5), comment, images
 * - response, responseDate, isVerified, createdAt
 */
public class Review {
    // TODO: Implémenter avec @Document (MongoDB)
}
