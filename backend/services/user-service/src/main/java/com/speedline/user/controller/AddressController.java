package com.speedline.user.controller;

import org.springframework.web.bind.annotation.*;

/**
 * REST Controller pour Address
 * 
 * Endpoints:
 * GET    /addresses/{id}
 * PUT    /addresses/{id}
 * DELETE /addresses/{id}
 */
@RestController
@RequestMapping("/addresses")
public class AddressController {
    // TODO: Implémenter
}
