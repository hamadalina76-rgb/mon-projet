package com.speedline.user.controller;

import org.springframework.web.bind.annotation.*;

/**
 * REST Controller pour Courier
 * 
 * Endpoints:
 * GET    /couriers/{id}
 * PUT    /couriers/{id}
 * PUT    /couriers/{id}/availability
 * GET    /couriers/{id}/statistics
 * POST   /couriers/{id}/documents
 */
@RestController
@RequestMapping("/couriers")
public class CourierController {
    // TODO: Implémenter
}
