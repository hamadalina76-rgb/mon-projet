package com.speedline.user.domain;

/**
 * Entité Address
 * - id, userId, type, label, street, building, floor, apartment
 * - city, postalCode, country, location (GPS), instructions, isDefault
 */
public class Address {
    // TODO: Implémenter avec @Entity
}
