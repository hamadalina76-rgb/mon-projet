package com.speedline.support.domain;

/**
 * Entité SupportTicket (PostgreSQL)
 * - id, ticketNumber, userId, userType (CUSTOMER/COURIER/PARTNER)
 * - orderId, category (ORDER, DELIVERY, PAYMENT, ACCOUNT, OTHER)
 * - priority, status, subject, description
 * - assignedTo, createdAt, updatedAt, resolvedAt
 */
public class SupportTicket {
    // TODO: Implémenter avec @Entity
}
