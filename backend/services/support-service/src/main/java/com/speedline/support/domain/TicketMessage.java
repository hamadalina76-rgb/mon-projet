package com.speedline.support.domain;

/**
 * Entité TicketMessage (PostgreSQL)
 * - id, ticketId, senderId, senderType
 * - message, attachments, createdAt
 */
public class TicketMessage {
    // TODO: Implémenter avec @Entity
}
