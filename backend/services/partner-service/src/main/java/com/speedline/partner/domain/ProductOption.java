package com.speedline.partner.domain;

/**
 * Entité ProductOption (ex: taille, cuisson)
 * - id, productId, name, type, isRequired, minSelection, maxSelection
 */
public class ProductOption {
    // TODO: Implémenter avec @Entity
}
