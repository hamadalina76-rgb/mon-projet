package com.speedline.notification.domain;

/**
 * Entité Notification (MongoDB)
 * - id, userId, type, title, message, data, isRead, channel, createdAt
 */
public class Notification {
    // TODO: Implémenter avec @Document (MongoDB)
}
