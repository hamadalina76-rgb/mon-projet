# ============================================================================
# CONFIGURATION PRODUCTION - API GATEWAY SPEEDLINE
# ============================================================================
# Ce fichier surcharge UNIQUEMENT les valeurs production
# Les routes viennent d'application.yml
server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
  tomcat:
    threads:
      max: 500
      min-spare: 50
spring:
  main:
    lazy-initialization: false
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 50
          max-idle: 25
          min-idle: 20
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE:http://eureka:eureka123@eureka:8761/eureka/}
    registry-fetch-interval-seconds: 30
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 30
    lease-expiration-duration-in-seconds: 90
    instance-id: ${spring.application.name}:${random.value}
management:
  server:
    port: ${MANAGEMENT_SERVER_PORT:9090}
  endpoints:
    web:
      exposure:
        include: health,info,gateway,metrics
      base-path: /actuator
  endpoint:
    health:
      show-details: when-authorized
      show-components: when-authorized
      probes:
        enabled: true
logging:
  level:
    root: ERROR
    org.springframework: WARN
    org.springframework.cloud.gateway: WARN
    org.springframework.security: WARN
    com.speedline: WARN
    com.speedline.gateway: WARN
    com.speedline.auth: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/api-gateway.log
resilience4j:
  circuitbreaker:
    instances:
      default:
        minimum-number-of-calls: 50
        failure-rate-threshold: 30
        wait-duration-in-open-state: 120000
        permitted-number-of-calls-in-half-open-state: 20
